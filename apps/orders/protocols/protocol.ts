import { prototype } from 'events';
import 'protocol';

function protocol() {
  let protocol_ip = "0.0.0.0:5500";

  if (protocol_ip == "method.VIEWDATA") {
    console.log(protocol_ip);
  }

  if (protocol_ip == "localhost:server.1676:varnet.com/auth2") {
    let protocol_method = "url, encoder_protocols:localhost:server.1676, decore.setFormatMetaData('decode, encode')";

    protocol_method == 'protocol_ip.length';
    prototype;
  }
}

