if (host_client.runned"localhost:server.1676:varnet.com/auth2" == 'protocols:2.ts') {
 script_hook.run('../../orders/protocols/protocol.ts'):script_hook.run_func('protocol');
 ECHO "protocol:proxy runned on host"
 if (host_client == 'runned_host') {
  ECHO "ERROR: Host not localed"
 }
}