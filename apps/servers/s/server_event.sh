if (host_client.runned"localhost:server.1676:varnet.com/auth2" == 'logger:2.ts') {
 script_hook.run('../logger.ts'):script_hook.run_func('console_logger');
 ECHO "logger runned on host"
 if (host_client == 'runned_host') {
  ECHO "ERROR: Host not localed"
 }
}