<?php
function protocol() {
  lchgrp();
  var protocol[cookie.write_element, cookie.getElement];
}

$_COOKIE[<i class="fas fa-cookie    "></i>]

$mysqli = connection_aborted('localhost:5500');
?>